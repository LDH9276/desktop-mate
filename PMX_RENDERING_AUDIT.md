# PMX 렌더링·긴 머리 물리 점검 (2026-09-14)

대상: Denia red/blue PMX, 사용자 확인 모델 桑多涅.pmx.
원본 PMX 및 텍스처 파일은 변경하지 않았다.

## 외곽선

- Three.js r169 OutlineEffect는 텍스처 알파를 읽지 않아 투명한 머리카락/하이라이트 면에도 실선이 생겼다. 로컬 MIT 포크에서 알파·알파맵·알파 테스트를 반영했다.
- 기존 화면 공간 확장은 깊이와 w도 변경하여 얇은 머리카락 면을 선이 파고들었다. x/y 방향으로 확장하고 선을 원래 면 뒤에 두도록 수정했다.
- Denia의 앞머리/뒷머리에는 완전히 같은 면을 복제한 하이라이트 레이어가 있다. 정점 속성 및 모프까지 일치하는 투명 레이어만 감지해 깊이 쓰기와 중복 외곽선을 해제했다. 표면을 조금 띄워 깊이 충돌을 줄였다.
- 정점별 edgeRatio 누락도 조사했으나 Denia는 모두 1이므로 이번 원인이 아니다.
- 색상·굵기·켜기/끄기 설정은 유지된다.

비교: artifacts/pmx-rendering/0-before-front.png → 0-after-front.png.
두 모델의 정면 및 좌우 각도를 렌더링했고 투명/반투명 픽셀 검사를 수행했다.

## 조명과 그림자

현재 앱에는 실시간 그림자가 켜져 있지 않다. 강한 환경광/보조광,
PMX의 toon·matcap 설정도 원본 예시와 다른 명암의 원인이다.
시험용 2048 shadow map을 적용하니 털 장식에 격자형 자기 그림자가 발생했다.
이 시험 설정은 앱에 적용하지 않았다. 실제 적용에는 재질별 그림자 투사/수신과
털·투명 장식의 깊이 처리를 별도로 조정해야 한다.
시험 이미지: artifacts/pmx-rendering/0-shadow-study.png.

## 긴 머리 물리

桑多涅에는 물리 바디 571개, 관절 873개가 있으며 머리 아래에 동적 바디
113개가 있다. 이 중 92개는 몸통/어깨 그룹과 충돌하도록 설정되어 있다.
충돌 마스크 0인 보조 추는 제작 설정을 유지했다.

앱의 두 문제를 수정했다.

1. 머리 아래의 모든 뼈에 적용하던 장식용 회전 제한에서 실제 머리카락을 분리했다.
   명시적인 리본·장식의 완화 처리는 유지한다.
2. 회전 보간의 목적 객체를 두 번째 입력으로도 사용하여 물리 회전이 덮어써지는
   오류를 수정했다. 이 수정은 같은 경로를 쓰는 의상 물리에도 적용된다.

실제 모델을 240프레임씩 시험했을 때 검사 대상 머리카락의 최대 로컬 회전은
수정 전 0도, 수정 후 가중치 35%에서 약 9.2도, 100%에서 약 26.4도였다.
각 실행은 물리 259스텝을 수행했고 비정상 수치는 없었다.
이는 제한된 시험 동작의 결과이며 모든 동작에서 관통이 없다는 의미는 아니다.
35% 가중치는 충돌 결과도 원래 자세와 섞으므로 관통 방지의 강도가 낮아질 수 있다.

## 재검증

- npm test
- npm run build
- node scripts/test-pmx-rendering.mjs
- node scripts/test-pmx-hair.mjs
- node scripts/test-pmx-transparency.mjs
- node scripts/test-mmd-toon-outline.mjs

실제 모델 파일을 사용하는 스크립트는 로컬 Denia.zip 및
artifacts/user-collision-model/桑多涅.pmx가 필요하다.
실행 결과와 이미지는 artifacts/pmx-rendering에 저장된다.
로컬 실행 바로가기인 Mate 실행.vbs는 dist 빌드를 사용한다.
